<?php
session_start();
    // $count = 0;
    // if (($handle = fopen("accounts_db.csv", "r")) !== FALSE) {
    //   while (($csvadata = fgetcsv($handle, 0, ",")) !== FALSE) {      
    //     $data[$count++] = $csvadata;
    //     }
    //     fclose($handle);
        
    //     unset($data[2]);
    // }
    // $fp = fopen('accounts_db.csv', 'w');
    // foreach ($data as $fields) {
    //     fputcsv($fp, $fields);
    // }
    // fclose($fp);
    // header('location:admin.php')
?>